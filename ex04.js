let nombreMultiplication = 5;

// pour afficher la table de multiplication du nombre
for (let i = 1; i <= 10; i++) {
    // chaque ligne de la table
    console.log(`${i} x ${nombreMultiplication} = ${i * nombreMultiplication}`);
}
